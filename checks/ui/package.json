{
  "name": "groundwork-ui-checks",
  "private": true,
  "description": "dev-only: playwright for check_cockpit_live.mjs. npm --prefix checks/ui install && npx playwright install chromium webkit",
  "type": "module",
  "devDependencies": { "playwright": "^1.62.1" }
}
